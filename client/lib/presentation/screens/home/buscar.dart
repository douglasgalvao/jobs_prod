import 'package:flutter/material.dart';

class HomeBuscar extends StatelessWidget {
  const HomeBuscar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Essa é a Página 2!'),
    );
  }
}
